name: firebase_feature_flag
description: "A Flutter package for managing feature flags using Firebase Realtime Database."
version: 1.0.2
homepage: https://github.com/karrarfalih/firebase_feature_flag

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  firebase_database: '>=9.0.0 <=10.4.0'
  flutter:
    sdk: flutter
  hive: '>=2.0.0 <=2.2.3'
  rxdart: '>=0.27.0 <=0.27.7'

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
